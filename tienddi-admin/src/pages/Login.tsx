import React from "react";
import Formulario from "../components/Formulario";

const Login = () => {
  return (
    <div className="p-10 bg-gradient-to-r from-yellow-100 to-amber-100 h-screen flex justify-between items-center">
      <div></div>
      <Formulario />
    </div>
  );
};

export default Login;
